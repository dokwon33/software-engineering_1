<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />  <!-- 배경색을 흰색으로 설정 -->
    <corners android:radius="4dp" />  <!-- 둥근 모서리 -->
    <stroke android:color="#000000" android:width="2dp" /> <!-- 검은색 테두리 -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
